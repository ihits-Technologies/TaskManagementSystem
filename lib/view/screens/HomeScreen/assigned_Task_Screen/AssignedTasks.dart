import 'package:flutter/material.dart';

class AssignedScren extends StatelessWidget {
  const AssignedScren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(""
            "Assigned Task Screen"),
      ),
    );
  }
}
